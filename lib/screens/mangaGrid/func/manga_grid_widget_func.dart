import 'package:flutter/material.dart';

// Widget itemBuilder(BuildContext context, int index, List<Media>? lista) =>
//     HomeCardWidget(
//       lista: lista![index],
//     );

Widget separatorBuilder(BuildContext context, int index) =>
    const Divider(thickness: 1.2);
